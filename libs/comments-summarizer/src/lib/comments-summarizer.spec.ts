import { commentsSummarizer } from './comments-summarizer';

describe('commentsSummarizer', () => {
  it('should work', () => {
    expect(commentsSummarizer()).toEqual('comments-summarizer');
  });
});
